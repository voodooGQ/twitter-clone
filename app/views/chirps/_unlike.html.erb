<%= form_for(current_user.likes.find_by(chirp_id: chirp.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <span class="like-submit unlike glyphicon glyphicon-heart"></span>
<% end %>
