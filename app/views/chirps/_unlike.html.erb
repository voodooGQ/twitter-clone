<%= form_for(current_user.likes.find_by(chirp_id: chirp.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unlike", class: "btn btn-large btn-danger" %>
<% end %>
