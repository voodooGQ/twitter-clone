<%= form_for(current_user.followed_relationships.build(followed_user_id: @user.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :followed_user_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>
