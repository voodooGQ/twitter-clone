<%= form_for(current_user.followed_relationships.find_by(followed_user_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large btn-danger" %>
<% end %>
